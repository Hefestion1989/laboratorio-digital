# Inbox

Entrada temporal para archivos o ideas que todavia no tienen lugar definitivo.

La idea es vaciar esta carpeta cada tanto y mover cada cosa a `proyectos/`, `experimentos/`, `notas/` o `archivo/`.
